type Query {
    customers: [Customer]
    customersByName(name: String): [Customer]
}

type Customer {
    id: ID
    name: String
    age: Int
    orders: [Order]
}

type Order {
    id: ID
    customerId: ID
}

type Mutation {
    addCustomer(name: String!, age: Int!): Customer
}